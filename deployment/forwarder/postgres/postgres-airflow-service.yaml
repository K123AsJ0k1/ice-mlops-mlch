apiVersion: v1
kind: Service
metadata:
  name: postgres-airflow-service
  labels:
    app: postgres-airflow-service
spec:
  selector:
    app: postgres-airflow-server
  ports:
    - port: 5432
      targetPort: 5432