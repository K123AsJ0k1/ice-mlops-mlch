[project] 
name = "icebreaker"
version = "0.0.1"
description = "Abstraction functions that make integrating computing enviroments (ICE) easier"
dependencies = [
    "pydantic"
]

[tool.setuptools.packages.find]
where = ["src"]

[project.optional-dependencies]

notebook = [
    "apache-airflow-client==3.2.2",
    "kfp==2.16.1"
]

yaml = [
    "PyYAML"
]

env = [
    "python-decouple"
]

swift = [
    "python-swiftclient",
    "keystoneauth1",
    "python-keystoneclient"
]

data = [
    "pandas",
    "pyarrow"
]

lang_detect = [
    "fasttext-wheel"
]

code_detect = [
    "magika"
]

redis = [
    "redis==7.3.0"
]

flower = [
    "flower"
]

ray = [
    "ray[default]==2.49.2"
]

pararellism = [
    "numpy"
]

logging = [
    "mlflow==3.12.0"
]

rag = [
    "qdrant-client==1.18.0",
    "minio==7.2.20",
    "pymongo==4.17.0",
    "langchain==1.3.10"
]

all = [
    "icebreaker[notebook, env, swift, data, lang_detect, code_detect, redis, flower, ray, pararellism, logging, rag]"
]